<?php

/**
 * Application version.
 *
 * Increment $version_major for breaking changes or major feature releases.
 * Increment $version_minor for backwards-compatible additions and fixes.
 *
 * @var int $version_major
 * @var int $version_minor
 */
$version_major = 0;
$version_minor = 9;
$version_patch = 0;

/**
 * Full version string, e.g. "1.0"
 * @var string
 */
$version = "{$version_major}.{$version_minor}.{$version_patch}";

/**
 * Last agent version
 * @var string
 */
$agent_last_version = "0.7";